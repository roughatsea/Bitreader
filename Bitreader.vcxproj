<?xml version="1.0" encoding="utf-8"?> <!-- Declare XML version and text encoding so Visual Studio and MSBuild can read the elements below. Angle syntax: The < and > bound the XML declaration; the question marks mark it as declaration syntax specifying how the following file is decoded. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003"> <!-- Define an MSBuild C++ project; its imported targets build the sources listed later in this file. Angle syntax: The < and > bound the opening Project tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <ItemGroup Label="ProjectConfigurations"> <!-- Group available build options that the configuration-specific settings below will reference. Angle syntax: The < and > bound the opening ItemGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ProjectConfiguration Include="Debug|Win32"> <!-- Declare the Debug|Win32 build option; its configuration and platform values are specified immediately below. Angle syntax: The < and > bound the opening ProjectConfiguration tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <Configuration>Debug</Configuration> <!-- Select Debug behavior for the build option declared by its enclosing ProjectConfiguration. Angle syntax: The < and > bound both the opening and closing Configuration tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <Platform>Win32</Platform> <!-- Select the Win32 CPU platform for the enclosing project build option. Angle syntax: The < and > bound both the opening and closing Platform tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </ProjectConfiguration> <!-- Close ProjectConfiguration for Debug|Win32; following elements return to its parent. Angle syntax: The < and > bound a closing ProjectConfiguration tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ProjectConfiguration Include="Release|Win32"> <!-- Declare the Release|Win32 build option; its configuration and platform values are specified immediately below. Angle syntax: The < and > bound the opening ProjectConfiguration tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <Configuration>Release</Configuration> <!-- Select Release behavior for the build option declared by its enclosing ProjectConfiguration. Angle syntax: The < and > bound both the opening and closing Configuration tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <Platform>Win32</Platform> <!-- Select the Win32 CPU platform for the enclosing project build option. Angle syntax: The < and > bound both the opening and closing Platform tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </ProjectConfiguration> <!-- Close ProjectConfiguration for Release|Win32; following elements return to its parent. Angle syntax: The < and > bound a closing ProjectConfiguration tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ProjectConfiguration Include="Debug|x64"> <!-- Declare the Debug|x64 build option; its configuration and platform values are specified immediately below. Angle syntax: The < and > bound the opening ProjectConfiguration tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <Configuration>Debug</Configuration> <!-- Select Debug behavior for the build option declared by its enclosing ProjectConfiguration. Angle syntax: The < and > bound both the opening and closing Configuration tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <Platform>x64</Platform> <!-- Select the x64 CPU platform for the enclosing project build option. Angle syntax: The < and > bound both the opening and closing Platform tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </ProjectConfiguration> <!-- Close ProjectConfiguration for Debug|x64; following elements return to its parent. Angle syntax: The < and > bound a closing ProjectConfiguration tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ProjectConfiguration Include="Release|x64"> <!-- Declare the Release|x64 build option; its configuration and platform values are specified immediately below. Angle syntax: The < and > bound the opening ProjectConfiguration tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <Configuration>Release</Configuration> <!-- Select Release behavior for the build option declared by its enclosing ProjectConfiguration. Angle syntax: The < and > bound both the opening and closing Configuration tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <Platform>x64</Platform> <!-- Select the x64 CPU platform for the enclosing project build option. Angle syntax: The < and > bound both the opening and closing Platform tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </ProjectConfiguration> <!-- Close ProjectConfiguration for Release|x64; following elements return to its parent. Angle syntax: The < and > bound a closing ProjectConfiguration tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </ItemGroup> <!-- Close ProjectConfigurations ItemGroup; following elements return to its parent. Angle syntax: The < and > bound a closing ItemGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <PropertyGroup Label="Globals"> <!-- Group shared project identity and SDK properties consumed by the C++ imports below. Angle syntax: The < and > bound the opening PropertyGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <VCProjectVersion>18.0</VCProjectVersion> <!-- Select the Visual Studio C++ project format version used when opening this project. Angle syntax: The < and > bound both the opening and closing VCProjectVersion tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <Keyword>Win32Proj</Keyword> <!-- Identify this as a native Windows C++ project so Visual Studio uses its C++ project tools. Angle syntax: The < and > bound both the opening and closing Keyword tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ProjectGuid>{18cfa057-8804-43f9-8f88-50a0f19d9d64}</ProjectGuid> <!-- Assign a stable project identity used by Visual Studio to distinguish it from other projects. Angle syntax: The < and > bound both the opening and closing ProjectGuid tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <RootNamespace>Bitreader</RootNamespace> <!-- Set the project's default namespace label; explicit C++ namespace declarations remain in the source files. Angle syntax: The < and > bound both the opening and closing RootNamespace tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <WindowsTargetPlatformVersion>10.0</WindowsTargetPlatformVersion> <!-- Request the installed Windows 10 SDK family used by the compiler and linker imports below. Angle syntax: The < and > bound both the opening and closing WindowsTargetPlatformVersion tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </PropertyGroup> <!-- Close Globals PropertyGroup; following elements return to its parent. Angle syntax: The < and > bound a closing PropertyGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.Default.props" /> <!-- Load standard C++ defaults before defining this project's executable and toolset properties. Angle syntax: The < and > bound a self-closing Import element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|Win32'" Label="Configuration"> <!-- Group executable and toolset properties for the build selected by '$(Configuration)|$(Platform)'=='Debug|Win32'. Angle syntax: The < and > bound the opening PropertyGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ConfigurationType>Application</ConfigurationType> <!-- Build an executable application, matching the main function in the listed source files. Angle syntax: The < and > bound both the opening and closing ConfigurationType tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <UseDebugLibraries>true</UseDebugLibraries> <!-- Use debug runtime libraries for this debug build, supporting debugger inspection of the objects. Angle syntax: The < and > bound both the opening and closing UseDebugLibraries tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <PlatformToolset>v145</PlatformToolset> <!-- Select the v145 C++ compiler and linker toolset for compiling the sources listed below. Angle syntax: The < and > bound both the opening and closing PlatformToolset tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <CharacterSet>Unicode</CharacterSet> <!-- Select Unicode project macros; binary stream reads still use raw bytes in the C++ reader. Angle syntax: The < and > bound both the opening and closing CharacterSet tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </PropertyGroup> <!-- Close Configuration PropertyGroup selected by '$(Configuration)|$(Platform)'=='Debug|Win32'; following elements return to its parent. Angle syntax: The < and > bound a closing PropertyGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|Win32'" Label="Configuration"> <!-- Group executable and toolset properties for the build selected by '$(Configuration)|$(Platform)'=='Release|Win32'. Angle syntax: The < and > bound the opening PropertyGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ConfigurationType>Application</ConfigurationType> <!-- Build an executable application, matching the main function in the listed source files. Angle syntax: The < and > bound both the opening and closing ConfigurationType tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <UseDebugLibraries>false</UseDebugLibraries> <!-- Use release runtime libraries for this release build rather than the debug runtime. Angle syntax: The < and > bound both the opening and closing UseDebugLibraries tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <PlatformToolset>v145</PlatformToolset> <!-- Select the v145 C++ compiler and linker toolset for compiling the sources listed below. Angle syntax: The < and > bound both the opening and closing PlatformToolset tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <WholeProgramOptimization>true</WholeProgramOptimization> <!-- Enable cross-module optimization for this release configuration when compiler and linker settings permit it. Angle syntax: The < and > bound both the opening and closing WholeProgramOptimization tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <CharacterSet>Unicode</CharacterSet> <!-- Select Unicode project macros; binary stream reads still use raw bytes in the C++ reader. Angle syntax: The < and > bound both the opening and closing CharacterSet tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </PropertyGroup> <!-- Close Configuration PropertyGroup selected by '$(Configuration)|$(Platform)'=='Release|Win32'; following elements return to its parent. Angle syntax: The < and > bound a closing PropertyGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|x64'" Label="Configuration"> <!-- Group executable and toolset properties for the build selected by '$(Configuration)|$(Platform)'=='Debug|x64'. Angle syntax: The < and > bound the opening PropertyGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ConfigurationType>Application</ConfigurationType> <!-- Build an executable application, matching the main function in the listed source files. Angle syntax: The < and > bound both the opening and closing ConfigurationType tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <UseDebugLibraries>true</UseDebugLibraries> <!-- Use debug runtime libraries for this debug build, supporting debugger inspection of the objects. Angle syntax: The < and > bound both the opening and closing UseDebugLibraries tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <PlatformToolset>v145</PlatformToolset> <!-- Select the v145 C++ compiler and linker toolset for compiling the sources listed below. Angle syntax: The < and > bound both the opening and closing PlatformToolset tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <CharacterSet>Unicode</CharacterSet> <!-- Select Unicode project macros; binary stream reads still use raw bytes in the C++ reader. Angle syntax: The < and > bound both the opening and closing CharacterSet tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </PropertyGroup> <!-- Close Configuration PropertyGroup selected by '$(Configuration)|$(Platform)'=='Debug|x64'; following elements return to its parent. Angle syntax: The < and > bound a closing PropertyGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|x64'" Label="Configuration"> <!-- Group executable and toolset properties for the build selected by '$(Configuration)|$(Platform)'=='Release|x64'. Angle syntax: The < and > bound the opening PropertyGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ConfigurationType>Application</ConfigurationType> <!-- Build an executable application, matching the main function in the listed source files. Angle syntax: The < and > bound both the opening and closing ConfigurationType tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <UseDebugLibraries>false</UseDebugLibraries> <!-- Use release runtime libraries for this release build rather than the debug runtime. Angle syntax: The < and > bound both the opening and closing UseDebugLibraries tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <PlatformToolset>v145</PlatformToolset> <!-- Select the v145 C++ compiler and linker toolset for compiling the sources listed below. Angle syntax: The < and > bound both the opening and closing PlatformToolset tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <WholeProgramOptimization>true</WholeProgramOptimization> <!-- Enable cross-module optimization for this release configuration when compiler and linker settings permit it. Angle syntax: The < and > bound both the opening and closing WholeProgramOptimization tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <CharacterSet>Unicode</CharacterSet> <!-- Select Unicode project macros; binary stream reads still use raw bytes in the C++ reader. Angle syntax: The < and > bound both the opening and closing CharacterSet tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </PropertyGroup> <!-- Close Configuration PropertyGroup selected by '$(Configuration)|$(Platform)'=='Release|x64'; following elements return to its parent. Angle syntax: The < and > bound a closing PropertyGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.props" /> <!-- Load compiler and platform properties after the project selects its toolset. Angle syntax: The < and > bound a self-closing Import element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <ImportGroup Label="ExtensionSettings"> <!-- Group ExtensionSettings imports; entries here can extend the standard C++ build. Angle syntax: The < and > bound the opening ImportGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </ImportGroup> <!-- Close ExtensionSettings ImportGroup; following elements return to its parent. Angle syntax: The < and > bound a closing ImportGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <ImportGroup Label="Shared"> <!-- Group Shared imports; entries here can extend the standard C++ build. Angle syntax: The < and > bound the opening ImportGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </ImportGroup> <!-- Close Shared ImportGroup; following elements return to its parent. Angle syntax: The < and > bound a closing ImportGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <ImportGroup Label="PropertySheets" Condition="'$(Configuration)|$(Platform)'=='Debug|Win32'"> <!-- Group PropertySheets imports for '$(Configuration)|$(Platform)'=='Debug|Win32'; entries here can extend the standard C++ build. Angle syntax: The < and > bound the opening ImportGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <Import Project="$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props" Condition="exists('$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props')" Label="LocalAppDataPlatform" /> <!-- Load the platform's optional user property sheet only if it exists, extending this configuration's settings. Angle syntax: The < and > bound a self-closing Import element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </ImportGroup> <!-- Close PropertySheets ImportGroup selected by '$(Configuration)|$(Platform)'=='Debug|Win32'; following elements return to its parent. Angle syntax: The < and > bound a closing ImportGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <ImportGroup Label="PropertySheets" Condition="'$(Configuration)|$(Platform)'=='Release|Win32'"> <!-- Group PropertySheets imports for '$(Configuration)|$(Platform)'=='Release|Win32'; entries here can extend the standard C++ build. Angle syntax: The < and > bound the opening ImportGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <Import Project="$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props" Condition="exists('$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props')" Label="LocalAppDataPlatform" /> <!-- Load the platform's optional user property sheet only if it exists, extending this configuration's settings. Angle syntax: The < and > bound a self-closing Import element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </ImportGroup> <!-- Close PropertySheets ImportGroup selected by '$(Configuration)|$(Platform)'=='Release|Win32'; following elements return to its parent. Angle syntax: The < and > bound a closing ImportGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <ImportGroup Label="PropertySheets" Condition="'$(Configuration)|$(Platform)'=='Debug|x64'"> <!-- Group PropertySheets imports for '$(Configuration)|$(Platform)'=='Debug|x64'; entries here can extend the standard C++ build. Angle syntax: The < and > bound the opening ImportGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <Import Project="$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props" Condition="exists('$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props')" Label="LocalAppDataPlatform" /> <!-- Load the platform's optional user property sheet only if it exists, extending this configuration's settings. Angle syntax: The < and > bound a self-closing Import element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </ImportGroup> <!-- Close PropertySheets ImportGroup selected by '$(Configuration)|$(Platform)'=='Debug|x64'; following elements return to its parent. Angle syntax: The < and > bound a closing ImportGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <ImportGroup Label="PropertySheets" Condition="'$(Configuration)|$(Platform)'=='Release|x64'"> <!-- Group PropertySheets imports for '$(Configuration)|$(Platform)'=='Release|x64'; entries here can extend the standard C++ build. Angle syntax: The < and > bound the opening ImportGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <Import Project="$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props" Condition="exists('$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props')" Label="LocalAppDataPlatform" /> <!-- Load the platform's optional user property sheet only if it exists, extending this configuration's settings. Angle syntax: The < and > bound a self-closing Import element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </ImportGroup> <!-- Close PropertySheets ImportGroup selected by '$(Configuration)|$(Platform)'=='Release|x64'; following elements return to its parent. Angle syntax: The < and > bound a closing ImportGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <PropertyGroup Label="UserMacros" /> <!-- Provide an empty user-macro group where custom MSBuild variables could be added. Angle syntax: The < and > bound a self-closing PropertyGroup element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <ItemDefinitionGroup Condition="'$(Configuration)|$(Platform)'=='Debug|Win32'"> <!-- Set defaults for compiler, linker, and manifest tasks when '$(Configuration)|$(Platform)'=='Debug|Win32'. Angle syntax: The < and > bound the opening ItemDefinitionGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ClCompile> <!-- Group compiler options applied to the source files listed in the project's item groups. Angle syntax: The < and > bound the opening ClCompile tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <WarningLevel>Level3</WarningLevel> <!-- Select the compiler warning level for the source files in this enclosing build-settings group. Angle syntax: The < and > bound both the opening and closing WarningLevel tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <SDLCheck>true</SDLCheck> <!-- Enable additional compiler safety diagnostics for the parser, reader, and other listed sources. Angle syntax: The < and > bound both the opening and closing SDLCheck tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <PreprocessorDefinitions>WIN32;_DEBUG;_CONSOLE;%(PreprocessorDefinitions)</PreprocessorDefinitions> <!-- Define WIN32, _DEBUG, _CONSOLE for this build and preserve inherited macros through the final metadata expression. Angle syntax: The < and > bound both the opening and closing PreprocessorDefinitions tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <ConformanceMode>true</ConformanceMode> <!-- Enable standards-conforming C++ behavior for templates and the classes in the project. Angle syntax: The < and > bound both the opening and closing ConformanceMode tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <LanguageStandard>stdcpp20</LanguageStandard> <!-- Compile the sources as C++20, matching the project's documented language version. Angle syntax: The < and > bound both the opening and closing LanguageStandard tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </ClCompile> <!-- Close ClCompile; following elements return to its parent. Angle syntax: The < and > bound a closing ClCompile tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <Link> <!-- Group linker options that combine compiled objects into the executable. Angle syntax: The < and > bound the opening Link tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <SubSystem>Console</SubSystem> <!-- Build a console program so its main function and cout/cerr output work from PowerShell. Angle syntax: The < and > bound both the opening and closing SubSystem tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <GenerateDebugInformation>true</GenerateDebugInformation> <!-- Generate linker debugging information so Visual Studio can inspect parser calls and model objects. Angle syntax: The < and > bound both the opening and closing GenerateDebugInformation tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </Link> <!-- Close Link; following elements return to its parent. Angle syntax: The < and > bound a closing Link tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <Manifest> <!-- Group Windows application-manifest options embedded in the linked executable. Angle syntax: The < and > bound the opening Manifest tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <EnableSegmentHeap>true</EnableSegmentHeap> <!-- Request the Windows segment heap in the executable manifest for process allocations. Angle syntax: The < and > bound both the opening and closing EnableSegmentHeap tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </Manifest> <!-- Close Manifest; following elements return to its parent. Angle syntax: The < and > bound a closing Manifest tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </ItemDefinitionGroup> <!-- Close ItemDefinitionGroup selected by '$(Configuration)|$(Platform)'=='Debug|Win32'; following elements return to its parent. Angle syntax: The < and > bound a closing ItemDefinitionGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <ItemDefinitionGroup Condition="'$(Configuration)|$(Platform)'=='Release|Win32'"> <!-- Set defaults for compiler, linker, and manifest tasks when '$(Configuration)|$(Platform)'=='Release|Win32'. Angle syntax: The < and > bound the opening ItemDefinitionGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ClCompile> <!-- Group compiler options applied to the source files listed in the project's item groups. Angle syntax: The < and > bound the opening ClCompile tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <WarningLevel>Level3</WarningLevel> <!-- Select the compiler warning level for the source files in this enclosing build-settings group. Angle syntax: The < and > bound both the opening and closing WarningLevel tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <FunctionLevelLinking>true</FunctionLevelLinking> <!-- Emit separate function sections so the release linker can remove unused functions. Angle syntax: The < and > bound both the opening and closing FunctionLevelLinking tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <IntrinsicFunctions>true</IntrinsicFunctions> <!-- Allow compiler-supported intrinsic implementations of functions in this release build. Angle syntax: The < and > bound both the opening and closing IntrinsicFunctions tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <SDLCheck>true</SDLCheck> <!-- Enable additional compiler safety diagnostics for the parser, reader, and other listed sources. Angle syntax: The < and > bound both the opening and closing SDLCheck tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <PreprocessorDefinitions>WIN32;NDEBUG;_CONSOLE;%(PreprocessorDefinitions)</PreprocessorDefinitions> <!-- Define WIN32, NDEBUG, _CONSOLE for this build and preserve inherited macros through the final metadata expression. Angle syntax: The < and > bound both the opening and closing PreprocessorDefinitions tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <ConformanceMode>true</ConformanceMode> <!-- Enable standards-conforming C++ behavior for templates and the classes in the project. Angle syntax: The < and > bound both the opening and closing ConformanceMode tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <LanguageStandard>stdcpp20</LanguageStandard> <!-- Compile the sources as C++20, matching the project's documented language version. Angle syntax: The < and > bound both the opening and closing LanguageStandard tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </ClCompile> <!-- Close ClCompile; following elements return to its parent. Angle syntax: The < and > bound a closing ClCompile tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <Link> <!-- Group linker options that combine compiled objects into the executable. Angle syntax: The < and > bound the opening Link tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <SubSystem>Console</SubSystem> <!-- Build a console program so its main function and cout/cerr output work from PowerShell. Angle syntax: The < and > bound both the opening and closing SubSystem tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <GenerateDebugInformation>true</GenerateDebugInformation> <!-- Generate linker debugging information so Visual Studio can inspect parser calls and model objects. Angle syntax: The < and > bound both the opening and closing GenerateDebugInformation tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </Link> <!-- Close Link; following elements return to its parent. Angle syntax: The < and > bound a closing Link tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <Manifest> <!-- Group Windows application-manifest options embedded in the linked executable. Angle syntax: The < and > bound the opening Manifest tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <EnableSegmentHeap>true</EnableSegmentHeap> <!-- Request the Windows segment heap in the executable manifest for process allocations. Angle syntax: The < and > bound both the opening and closing EnableSegmentHeap tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </Manifest> <!-- Close Manifest; following elements return to its parent. Angle syntax: The < and > bound a closing Manifest tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </ItemDefinitionGroup> <!-- Close ItemDefinitionGroup selected by '$(Configuration)|$(Platform)'=='Release|Win32'; following elements return to its parent. Angle syntax: The < and > bound a closing ItemDefinitionGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <ItemDefinitionGroup Condition="'$(Configuration)|$(Platform)'=='Debug|x64'"> <!-- Set defaults for compiler, linker, and manifest tasks when '$(Configuration)|$(Platform)'=='Debug|x64'. Angle syntax: The < and > bound the opening ItemDefinitionGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ClCompile> <!-- Group compiler options applied to the source files listed in the project's item groups. Angle syntax: The < and > bound the opening ClCompile tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <WarningLevel>Level3</WarningLevel> <!-- Select the compiler warning level for the source files in this enclosing build-settings group. Angle syntax: The < and > bound both the opening and closing WarningLevel tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <SDLCheck>true</SDLCheck> <!-- Enable additional compiler safety diagnostics for the parser, reader, and other listed sources. Angle syntax: The < and > bound both the opening and closing SDLCheck tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <PreprocessorDefinitions>_DEBUG;_CONSOLE;%(PreprocessorDefinitions)</PreprocessorDefinitions> <!-- Define _DEBUG, _CONSOLE for this build and preserve inherited macros through the final metadata expression. Angle syntax: The < and > bound both the opening and closing PreprocessorDefinitions tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <ConformanceMode>true</ConformanceMode> <!-- Enable standards-conforming C++ behavior for templates and the classes in the project. Angle syntax: The < and > bound both the opening and closing ConformanceMode tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <LanguageStandard>stdcpp20</LanguageStandard> <!-- Compile the sources as C++20, matching the project's documented language version. Angle syntax: The < and > bound both the opening and closing LanguageStandard tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </ClCompile> <!-- Close ClCompile; following elements return to its parent. Angle syntax: The < and > bound a closing ClCompile tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <Link> <!-- Group linker options that combine compiled objects into the executable. Angle syntax: The < and > bound the opening Link tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <SubSystem>Console</SubSystem> <!-- Build a console program so its main function and cout/cerr output work from PowerShell. Angle syntax: The < and > bound both the opening and closing SubSystem tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <GenerateDebugInformation>true</GenerateDebugInformation> <!-- Generate linker debugging information so Visual Studio can inspect parser calls and model objects. Angle syntax: The < and > bound both the opening and closing GenerateDebugInformation tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </Link> <!-- Close Link; following elements return to its parent. Angle syntax: The < and > bound a closing Link tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <Manifest> <!-- Group Windows application-manifest options embedded in the linked executable. Angle syntax: The < and > bound the opening Manifest tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <EnableSegmentHeap>true</EnableSegmentHeap> <!-- Request the Windows segment heap in the executable manifest for process allocations. Angle syntax: The < and > bound both the opening and closing EnableSegmentHeap tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </Manifest> <!-- Close Manifest; following elements return to its parent. Angle syntax: The < and > bound a closing Manifest tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </ItemDefinitionGroup> <!-- Close ItemDefinitionGroup selected by '$(Configuration)|$(Platform)'=='Debug|x64'; following elements return to its parent. Angle syntax: The < and > bound a closing ItemDefinitionGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <ItemDefinitionGroup Condition="'$(Configuration)|$(Platform)'=='Release|x64'"> <!-- Set defaults for compiler, linker, and manifest tasks when '$(Configuration)|$(Platform)'=='Release|x64'. Angle syntax: The < and > bound the opening ItemDefinitionGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ClCompile> <!-- Group compiler options applied to the source files listed in the project's item groups. Angle syntax: The < and > bound the opening ClCompile tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <WarningLevel>Level3</WarningLevel> <!-- Select the compiler warning level for the source files in this enclosing build-settings group. Angle syntax: The < and > bound both the opening and closing WarningLevel tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <FunctionLevelLinking>true</FunctionLevelLinking> <!-- Emit separate function sections so the release linker can remove unused functions. Angle syntax: The < and > bound both the opening and closing FunctionLevelLinking tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <IntrinsicFunctions>true</IntrinsicFunctions> <!-- Allow compiler-supported intrinsic implementations of functions in this release build. Angle syntax: The < and > bound both the opening and closing IntrinsicFunctions tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <SDLCheck>true</SDLCheck> <!-- Enable additional compiler safety diagnostics for the parser, reader, and other listed sources. Angle syntax: The < and > bound both the opening and closing SDLCheck tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <PreprocessorDefinitions>NDEBUG;_CONSOLE;%(PreprocessorDefinitions)</PreprocessorDefinitions> <!-- Define NDEBUG, _CONSOLE for this build and preserve inherited macros through the final metadata expression. Angle syntax: The < and > bound both the opening and closing PreprocessorDefinitions tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <ConformanceMode>true</ConformanceMode> <!-- Enable standards-conforming C++ behavior for templates and the classes in the project. Angle syntax: The < and > bound both the opening and closing ConformanceMode tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <LanguageStandard>stdcpp20</LanguageStandard> <!-- Compile the sources as C++20, matching the project's documented language version. Angle syntax: The < and > bound both the opening and closing LanguageStandard tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </ClCompile> <!-- Close ClCompile; following elements return to its parent. Angle syntax: The < and > bound a closing ClCompile tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <Link> <!-- Group linker options that combine compiled objects into the executable. Angle syntax: The < and > bound the opening Link tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <SubSystem>Console</SubSystem> <!-- Build a console program so its main function and cout/cerr output work from PowerShell. Angle syntax: The < and > bound both the opening and closing SubSystem tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <GenerateDebugInformation>true</GenerateDebugInformation> <!-- Generate linker debugging information so Visual Studio can inspect parser calls and model objects. Angle syntax: The < and > bound both the opening and closing GenerateDebugInformation tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </Link> <!-- Close Link; following elements return to its parent. Angle syntax: The < and > bound a closing Link tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <Manifest> <!-- Group Windows application-manifest options embedded in the linked executable. Angle syntax: The < and > bound the opening Manifest tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <EnableSegmentHeap>true</EnableSegmentHeap> <!-- Request the Windows segment heap in the executable manifest for process allocations. Angle syntax: The < and > bound both the opening and closing EnableSegmentHeap tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </Manifest> <!-- Close Manifest; following elements return to its parent. Angle syntax: The < and > bound a closing Manifest tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </ItemDefinitionGroup> <!-- Close ItemDefinitionGroup selected by '$(Configuration)|$(Platform)'=='Release|x64'; following elements return to its parent. Angle syntax: The < and > bound a closing ItemDefinitionGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <ItemGroup> <!-- Group related project items, such as source files, headers, or Solution Explorer folders. Angle syntax: The < and > bound the opening ItemGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ClCompile Include="Bitreader.cpp" /> <!-- Compile the command-line entry point and hierarchy printer as part of this executable. Angle syntax: The < and > bound a self-closing ClCompile element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ClCompile Include="BinaryParser.cpp" /> <!-- Compile the field-to-setter mappings and parent-child assembly functions as part of this executable. Angle syntax: The < and > bound a self-closing ClCompile element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ClCompile Include="io\BitReader.cpp"> <!-- Compile the implementation that consumes bits across byte boundaries as part of this executable. Angle syntax: The < and > bound the opening ClCompile tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <ObjectFileName>$(IntDir)StreamBitReader.obj</ObjectFileName> <!-- Give io/BitReader.cpp a distinct object name so it cannot overwrite Bitreader.cpp's object on Windows. Angle syntax: The < and > bound both the opening and closing ObjectFileName tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </ClCompile> <!-- Close ClCompile for io\BitReader.cpp; following elements return to its parent. Angle syntax: The < and > bound a closing ClCompile tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </ItemGroup> <!-- Close ItemGroup; following elements return to its parent. Angle syntax: The < and > bound a closing ItemGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <ItemGroup> <!-- Group related project items, such as source files, headers, or Solution Explorer folders. Angle syntax: The < and > bound the opening ItemGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ClInclude Include="BinaryParser.h" /> <!-- Register the public parser declarations with Visual Studio; the C++ include directives determine compilation dependencies. Angle syntax: The < and > bound a self-closing ClInclude element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ClInclude Include="io\BitReader.h" /> <!-- Register the stream reader declaration and its cached-bit state with Visual Studio; the C++ include directives determine compilation dependencies. Angle syntax: The < and > bound a self-closing ClInclude element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ClInclude Include="models\BinaryDocument.h" /> <!-- Register the root model that owns the header and records with Visual Studio; the C++ include directives determine compilation dependencies. Angle syntax: The < and > bound a self-closing ClInclude element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ClInclude Include="models\FileHeader.h" /> <!-- Register the header model that owns its FormatInfo child with Visual Studio; the C++ include directives determine compilation dependencies. Angle syntax: The < and > bound a self-closing ClInclude element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ClInclude Include="models\FormatInfo.h" /> <!-- Register the version and tagged-flag leaf model with Visual Studio; the C++ include directives determine compilation dependencies. Angle syntax: The < and > bound a self-closing ClInclude element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ClInclude Include="models\Measurement.h" /> <!-- Register the magnitude and exponent leaf model with Visual Studio; the C++ include directives determine compilation dependencies. Angle syntax: The < and > bound a self-closing ClInclude element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ClInclude Include="models\Record.h" /> <!-- Register the record model that owns metadata and measurement children with Visual Studio; the C++ include directives determine compilation dependencies. Angle syntax: The < and > bound a self-closing ClInclude element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ClInclude Include="models\RecordMetadata.h" /> <!-- Register the ID, category, and active-flag leaf model with Visual Studio; the C++ include directives determine compilation dependencies. Angle syntax: The < and > bound a self-closing ClInclude element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </ItemGroup> <!-- Close ItemGroup; following elements return to its parent. Angle syntax: The < and > bound a closing ItemGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.targets" /> <!-- Load C++ build targets that compile and link the source items listed above. Angle syntax: The < and > bound a self-closing Import element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <ImportGroup Label="ExtensionTargets"> <!-- Group ExtensionTargets imports; entries here can extend the standard C++ build. Angle syntax: The < and > bound the opening ImportGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </ImportGroup> <!-- Close ExtensionTargets ImportGroup; following elements return to its parent. Angle syntax: The < and > bound a closing ImportGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
</Project> <!-- Close Project; following elements return to its parent. Angle syntax: The < and > bound a closing Project tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
