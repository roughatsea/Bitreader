<?xml version="1.0" encoding="utf-8"?> <!-- Declare XML version and text encoding so Visual Studio and MSBuild can read the elements below. -->
<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003"> <!-- Define an MSBuild C++ project; its imported targets build the sources listed later in this file. -->
  <ItemGroup Label="ProjectConfigurations"> <!-- Group available build options that the configuration-specific settings below will reference. -->
    <ProjectConfiguration Include="Debug|Win32"> <!-- Declare the Debug|Win32 build option; its configuration and platform values are specified immediately below. -->
      <Configuration>Debug</Configuration> <!-- Select Debug behavior for the build option declared by its enclosing ProjectConfiguration. -->
      <Platform>Win32</Platform> <!-- Select the Win32 CPU platform for the enclosing project build option. -->
    </ProjectConfiguration> <!-- Close ProjectConfiguration for Debug|Win32; following elements return to its parent. -->
    <ProjectConfiguration Include="Release|Win32"> <!-- Declare the Release|Win32 build option; its configuration and platform values are specified immediately below. -->
      <Configuration>Release</Configuration> <!-- Select Release behavior for the build option declared by its enclosing ProjectConfiguration. -->
      <Platform>Win32</Platform> <!-- Select the Win32 CPU platform for the enclosing project build option. -->
    </ProjectConfiguration> <!-- Close ProjectConfiguration for Release|Win32; following elements return to its parent. -->
    <ProjectConfiguration Include="Debug|x64"> <!-- Declare the Debug|x64 build option; its configuration and platform values are specified immediately below. -->
      <Configuration>Debug</Configuration> <!-- Select Debug behavior for the build option declared by its enclosing ProjectConfiguration. -->
      <Platform>x64</Platform> <!-- Select the x64 CPU platform for the enclosing project build option. -->
    </ProjectConfiguration> <!-- Close ProjectConfiguration for Debug|x64; following elements return to its parent. -->
    <ProjectConfiguration Include="Release|x64"> <!-- Declare the Release|x64 build option; its configuration and platform values are specified immediately below. -->
      <Configuration>Release</Configuration> <!-- Select Release behavior for the build option declared by its enclosing ProjectConfiguration. -->
      <Platform>x64</Platform> <!-- Select the x64 CPU platform for the enclosing project build option. -->
    </ProjectConfiguration> <!-- Close ProjectConfiguration for Release|x64; following elements return to its parent. -->
  </ItemGroup> <!-- Close ProjectConfigurations ItemGroup; following elements return to its parent. -->
  <PropertyGroup Label="Globals"> <!-- Group shared project identity and SDK properties consumed by the C++ imports below. -->
    <VCProjectVersion>18.0</VCProjectVersion> <!-- Select the Visual Studio C++ project format version used when opening this project. -->
    <Keyword>Win32Proj</Keyword> <!-- Identify this as a native Windows C++ project so Visual Studio uses its C++ project tools. -->
    <ProjectGuid>{18cfa057-8804-43f9-8f88-50a0f19d9d64}</ProjectGuid> <!-- Assign a stable project identity used by Visual Studio to distinguish it from other projects. -->
    <RootNamespace>Bitreader</RootNamespace> <!-- Set the project's default namespace label; explicit C++ namespace declarations remain in the source files. -->
    <WindowsTargetPlatformVersion>10.0</WindowsTargetPlatformVersion> <!-- Request the installed Windows 10 SDK family used by the compiler and linker imports below. -->
  </PropertyGroup> <!-- Close Globals PropertyGroup; following elements return to its parent. -->
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.Default.props" /> <!-- Load standard C++ defaults before defining this project's executable and toolset properties. -->
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|Win32'" Label="Configuration"> <!-- Group executable and toolset properties for the build selected by '$(Configuration)|$(Platform)'=='Debug|Win32'. -->
    <ConfigurationType>Application</ConfigurationType> <!-- Build an executable application, matching the main function in the listed source files. -->
    <UseDebugLibraries>true</UseDebugLibraries> <!-- Use debug runtime libraries for this debug build, supporting debugger inspection of the objects. -->
    <PlatformToolset>v145</PlatformToolset> <!-- Select the v145 C++ compiler and linker toolset for compiling the sources listed below. -->
    <CharacterSet>Unicode</CharacterSet> <!-- Select Unicode project macros; binary stream reads still use raw bytes in the C++ reader. -->
  </PropertyGroup> <!-- Close Configuration PropertyGroup selected by '$(Configuration)|$(Platform)'=='Debug|Win32'; following elements return to its parent. -->
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|Win32'" Label="Configuration"> <!-- Group executable and toolset properties for the build selected by '$(Configuration)|$(Platform)'=='Release|Win32'. -->
    <ConfigurationType>Application</ConfigurationType> <!-- Build an executable application, matching the main function in the listed source files. -->
    <UseDebugLibraries>false</UseDebugLibraries> <!-- Use release runtime libraries for this release build rather than the debug runtime. -->
    <PlatformToolset>v145</PlatformToolset> <!-- Select the v145 C++ compiler and linker toolset for compiling the sources listed below. -->
    <WholeProgramOptimization>true</WholeProgramOptimization> <!-- Enable cross-module optimization for this release configuration when compiler and linker settings permit it. -->
    <CharacterSet>Unicode</CharacterSet> <!-- Select Unicode project macros; binary stream reads still use raw bytes in the C++ reader. -->
  </PropertyGroup> <!-- Close Configuration PropertyGroup selected by '$(Configuration)|$(Platform)'=='Release|Win32'; following elements return to its parent. -->
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|x64'" Label="Configuration"> <!-- Group executable and toolset properties for the build selected by '$(Configuration)|$(Platform)'=='Debug|x64'. -->
    <ConfigurationType>Application</ConfigurationType> <!-- Build an executable application, matching the main function in the listed source files. -->
    <UseDebugLibraries>true</UseDebugLibraries> <!-- Use debug runtime libraries for this debug build, supporting debugger inspection of the objects. -->
    <PlatformToolset>v145</PlatformToolset> <!-- Select the v145 C++ compiler and linker toolset for compiling the sources listed below. -->
    <CharacterSet>Unicode</CharacterSet> <!-- Select Unicode project macros; binary stream reads still use raw bytes in the C++ reader. -->
  </PropertyGroup> <!-- Close Configuration PropertyGroup selected by '$(Configuration)|$(Platform)'=='Debug|x64'; following elements return to its parent. -->
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|x64'" Label="Configuration"> <!-- Group executable and toolset properties for the build selected by '$(Configuration)|$(Platform)'=='Release|x64'. -->
    <ConfigurationType>Application</ConfigurationType> <!-- Build an executable application, matching the main function in the listed source files. -->
    <UseDebugLibraries>false</UseDebugLibraries> <!-- Use release runtime libraries for this release build rather than the debug runtime. -->
    <PlatformToolset>v145</PlatformToolset> <!-- Select the v145 C++ compiler and linker toolset for compiling the sources listed below. -->
    <WholeProgramOptimization>true</WholeProgramOptimization> <!-- Enable cross-module optimization for this release configuration when compiler and linker settings permit it. -->
    <CharacterSet>Unicode</CharacterSet> <!-- Select Unicode project macros; binary stream reads still use raw bytes in the C++ reader. -->
  </PropertyGroup> <!-- Close Configuration PropertyGroup selected by '$(Configuration)|$(Platform)'=='Release|x64'; following elements return to its parent. -->
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.props" /> <!-- Load compiler and platform properties after the project selects its toolset. -->
  <ImportGroup Label="ExtensionSettings"> <!-- Group ExtensionSettings imports; entries here can extend the standard C++ build. -->
  </ImportGroup> <!-- Close ExtensionSettings ImportGroup; following elements return to its parent. -->
  <ImportGroup Label="Shared"> <!-- Group Shared imports; entries here can extend the standard C++ build. -->
  </ImportGroup> <!-- Close Shared ImportGroup; following elements return to its parent. -->
  <ImportGroup Label="PropertySheets" Condition="'$(Configuration)|$(Platform)'=='Debug|Win32'"> <!-- Group PropertySheets imports for '$(Configuration)|$(Platform)'=='Debug|Win32'; entries here can extend the standard C++ build. -->
    <Import Project="$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props" Condition="exists('$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props')" Label="LocalAppDataPlatform" /> <!-- Load the platform's optional user property sheet only if it exists, extending this configuration's settings. -->
  </ImportGroup> <!-- Close PropertySheets ImportGroup selected by '$(Configuration)|$(Platform)'=='Debug|Win32'; following elements return to its parent. -->
  <ImportGroup Label="PropertySheets" Condition="'$(Configuration)|$(Platform)'=='Release|Win32'"> <!-- Group PropertySheets imports for '$(Configuration)|$(Platform)'=='Release|Win32'; entries here can extend the standard C++ build. -->
    <Import Project="$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props" Condition="exists('$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props')" Label="LocalAppDataPlatform" /> <!-- Load the platform's optional user property sheet only if it exists, extending this configuration's settings. -->
  </ImportGroup> <!-- Close PropertySheets ImportGroup selected by '$(Configuration)|$(Platform)'=='Release|Win32'; following elements return to its parent. -->
  <ImportGroup Label="PropertySheets" Condition="'$(Configuration)|$(Platform)'=='Debug|x64'"> <!-- Group PropertySheets imports for '$(Configuration)|$(Platform)'=='Debug|x64'; entries here can extend the standard C++ build. -->
    <Import Project="$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props" Condition="exists('$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props')" Label="LocalAppDataPlatform" /> <!-- Load the platform's optional user property sheet only if it exists, extending this configuration's settings. -->
  </ImportGroup> <!-- Close PropertySheets ImportGroup selected by '$(Configuration)|$(Platform)'=='Debug|x64'; following elements return to its parent. -->
  <ImportGroup Label="PropertySheets" Condition="'$(Configuration)|$(Platform)'=='Release|x64'"> <!-- Group PropertySheets imports for '$(Configuration)|$(Platform)'=='Release|x64'; entries here can extend the standard C++ build. -->
    <Import Project="$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props" Condition="exists('$(UserRootDir)\Microsoft.Cpp.$(Platform).user.props')" Label="LocalAppDataPlatform" /> <!-- Load the platform's optional user property sheet only if it exists, extending this configuration's settings. -->
  </ImportGroup> <!-- Close PropertySheets ImportGroup selected by '$(Configuration)|$(Platform)'=='Release|x64'; following elements return to its parent. -->
  <PropertyGroup Label="UserMacros" /> <!-- Provide an empty user-macro group where custom MSBuild variables could be added. -->
  <ItemDefinitionGroup Condition="'$(Configuration)|$(Platform)'=='Debug|Win32'"> <!-- Set defaults for compiler, linker, and manifest tasks when '$(Configuration)|$(Platform)'=='Debug|Win32'. -->
    <ClCompile> <!-- Group compiler options applied to the source files listed in the project's item groups. -->
      <WarningLevel>Level3</WarningLevel> <!-- Select the compiler warning level for the source files in this enclosing build-settings group. -->
      <SDLCheck>true</SDLCheck> <!-- Enable additional compiler safety diagnostics for the parser, reader, and other listed sources. -->
      <PreprocessorDefinitions>WIN32;_DEBUG;_CONSOLE;%(PreprocessorDefinitions)</PreprocessorDefinitions> <!-- Define WIN32, _DEBUG, _CONSOLE for this build and preserve inherited macros through the final metadata expression. -->
      <ConformanceMode>true</ConformanceMode> <!-- Enable standards-conforming C++ behavior for templates and the classes in the project. -->
      <LanguageStandard>stdcpp20</LanguageStandard> <!-- Compile the sources as C++20, matching the project's documented language version. -->
    </ClCompile> <!-- Close ClCompile; following elements return to its parent. -->
    <Link> <!-- Group linker options that combine compiled objects into the executable. -->
      <SubSystem>Console</SubSystem> <!-- Build a console program so its main function and cout/cerr output work from PowerShell. -->
      <GenerateDebugInformation>true</GenerateDebugInformation> <!-- Generate linker debugging information so Visual Studio can inspect parser calls and model objects. -->
    </Link> <!-- Close Link; following elements return to its parent. -->
    <Manifest> <!-- Group Windows application-manifest options embedded in the linked executable. -->
      <EnableSegmentHeap>true</EnableSegmentHeap> <!-- Request the Windows segment heap in the executable manifest for process allocations. -->
    </Manifest> <!-- Close Manifest; following elements return to its parent. -->
  </ItemDefinitionGroup> <!-- Close ItemDefinitionGroup selected by '$(Configuration)|$(Platform)'=='Debug|Win32'; following elements return to its parent. -->
  <ItemDefinitionGroup Condition="'$(Configuration)|$(Platform)'=='Release|Win32'"> <!-- Set defaults for compiler, linker, and manifest tasks when '$(Configuration)|$(Platform)'=='Release|Win32'. -->
    <ClCompile> <!-- Group compiler options applied to the source files listed in the project's item groups. -->
      <WarningLevel>Level3</WarningLevel> <!-- Select the compiler warning level for the source files in this enclosing build-settings group. -->
      <FunctionLevelLinking>true</FunctionLevelLinking> <!-- Emit separate function sections so the release linker can remove unused functions. -->
      <IntrinsicFunctions>true</IntrinsicFunctions> <!-- Allow compiler-supported intrinsic implementations of functions in this release build. -->
      <SDLCheck>true</SDLCheck> <!-- Enable additional compiler safety diagnostics for the parser, reader, and other listed sources. -->
      <PreprocessorDefinitions>WIN32;NDEBUG;_CONSOLE;%(PreprocessorDefinitions)</PreprocessorDefinitions> <!-- Define WIN32, NDEBUG, _CONSOLE for this build and preserve inherited macros through the final metadata expression. -->
      <ConformanceMode>true</ConformanceMode> <!-- Enable standards-conforming C++ behavior for templates and the classes in the project. -->
      <LanguageStandard>stdcpp20</LanguageStandard> <!-- Compile the sources as C++20, matching the project's documented language version. -->
    </ClCompile> <!-- Close ClCompile; following elements return to its parent. -->
    <Link> <!-- Group linker options that combine compiled objects into the executable. -->
      <SubSystem>Console</SubSystem> <!-- Build a console program so its main function and cout/cerr output work from PowerShell. -->
      <GenerateDebugInformation>true</GenerateDebugInformation> <!-- Generate linker debugging information so Visual Studio can inspect parser calls and model objects. -->
    </Link> <!-- Close Link; following elements return to its parent. -->
    <Manifest> <!-- Group Windows application-manifest options embedded in the linked executable. -->
      <EnableSegmentHeap>true</EnableSegmentHeap> <!-- Request the Windows segment heap in the executable manifest for process allocations. -->
    </Manifest> <!-- Close Manifest; following elements return to its parent. -->
  </ItemDefinitionGroup> <!-- Close ItemDefinitionGroup selected by '$(Configuration)|$(Platform)'=='Release|Win32'; following elements return to its parent. -->
  <ItemDefinitionGroup Condition="'$(Configuration)|$(Platform)'=='Debug|x64'"> <!-- Set defaults for compiler, linker, and manifest tasks when '$(Configuration)|$(Platform)'=='Debug|x64'. -->
    <ClCompile> <!-- Group compiler options applied to the source files listed in the project's item groups. -->
      <WarningLevel>Level3</WarningLevel> <!-- Select the compiler warning level for the source files in this enclosing build-settings group. -->
      <SDLCheck>true</SDLCheck> <!-- Enable additional compiler safety diagnostics for the parser, reader, and other listed sources. -->
      <PreprocessorDefinitions>_DEBUG;_CONSOLE;%(PreprocessorDefinitions)</PreprocessorDefinitions> <!-- Define _DEBUG, _CONSOLE for this build and preserve inherited macros through the final metadata expression. -->
      <ConformanceMode>true</ConformanceMode> <!-- Enable standards-conforming C++ behavior for templates and the classes in the project. -->
      <LanguageStandard>stdcpp20</LanguageStandard> <!-- Compile the sources as C++20, matching the project's documented language version. -->
    </ClCompile> <!-- Close ClCompile; following elements return to its parent. -->
    <Link> <!-- Group linker options that combine compiled objects into the executable. -->
      <SubSystem>Console</SubSystem> <!-- Build a console program so its main function and cout/cerr output work from PowerShell. -->
      <GenerateDebugInformation>true</GenerateDebugInformation> <!-- Generate linker debugging information so Visual Studio can inspect parser calls and model objects. -->
    </Link> <!-- Close Link; following elements return to its parent. -->
    <Manifest> <!-- Group Windows application-manifest options embedded in the linked executable. -->
      <EnableSegmentHeap>true</EnableSegmentHeap> <!-- Request the Windows segment heap in the executable manifest for process allocations. -->
    </Manifest> <!-- Close Manifest; following elements return to its parent. -->
  </ItemDefinitionGroup> <!-- Close ItemDefinitionGroup selected by '$(Configuration)|$(Platform)'=='Debug|x64'; following elements return to its parent. -->
  <ItemDefinitionGroup Condition="'$(Configuration)|$(Platform)'=='Release|x64'"> <!-- Set defaults for compiler, linker, and manifest tasks when '$(Configuration)|$(Platform)'=='Release|x64'. -->
    <ClCompile> <!-- Group compiler options applied to the source files listed in the project's item groups. -->
      <WarningLevel>Level3</WarningLevel> <!-- Select the compiler warning level for the source files in this enclosing build-settings group. -->
      <FunctionLevelLinking>true</FunctionLevelLinking> <!-- Emit separate function sections so the release linker can remove unused functions. -->
      <IntrinsicFunctions>true</IntrinsicFunctions> <!-- Allow compiler-supported intrinsic implementations of functions in this release build. -->
      <SDLCheck>true</SDLCheck> <!-- Enable additional compiler safety diagnostics for the parser, reader, and other listed sources. -->
      <PreprocessorDefinitions>NDEBUG;_CONSOLE;%(PreprocessorDefinitions)</PreprocessorDefinitions> <!-- Define NDEBUG, _CONSOLE for this build and preserve inherited macros through the final metadata expression. -->
      <ConformanceMode>true</ConformanceMode> <!-- Enable standards-conforming C++ behavior for templates and the classes in the project. -->
      <LanguageStandard>stdcpp20</LanguageStandard> <!-- Compile the sources as C++20, matching the project's documented language version. -->
    </ClCompile> <!-- Close ClCompile; following elements return to its parent. -->
    <Link> <!-- Group linker options that combine compiled objects into the executable. -->
      <SubSystem>Console</SubSystem> <!-- Build a console program so its main function and cout/cerr output work from PowerShell. -->
      <GenerateDebugInformation>true</GenerateDebugInformation> <!-- Generate linker debugging information so Visual Studio can inspect parser calls and model objects. -->
    </Link> <!-- Close Link; following elements return to its parent. -->
    <Manifest> <!-- Group Windows application-manifest options embedded in the linked executable. -->
      <EnableSegmentHeap>true</EnableSegmentHeap> <!-- Request the Windows segment heap in the executable manifest for process allocations. -->
    </Manifest> <!-- Close Manifest; following elements return to its parent. -->
  </ItemDefinitionGroup> <!-- Close ItemDefinitionGroup selected by '$(Configuration)|$(Platform)'=='Release|x64'; following elements return to its parent. -->
  <ItemGroup> <!-- Group related project items, such as source files, headers, or Solution Explorer folders. -->
    <ClCompile Include="Bitreader.cpp" /> <!-- Compile the command-line entry point and hierarchy printer as part of this executable. -->
    <ClCompile Include="BinaryParser.cpp" /> <!-- Compile the field-to-setter mappings and parent-child assembly functions as part of this executable. -->
    <ClCompile Include="io\BitReader.cpp"> <!-- Compile the implementation that consumes bits across byte boundaries as part of this executable. -->
      <ObjectFileName>$(IntDir)StreamBitReader.obj</ObjectFileName> <!-- Give io/BitReader.cpp a distinct object name so it cannot overwrite Bitreader.cpp's object on Windows. -->
    </ClCompile> <!-- Close ClCompile for io\BitReader.cpp; following elements return to its parent. -->
  </ItemGroup> <!-- Close ItemGroup; following elements return to its parent. -->
  <ItemGroup> <!-- Group related project items, such as source files, headers, or Solution Explorer folders. -->
    <ClInclude Include="BinaryParser.h" /> <!-- Register the public parser declarations with Visual Studio; the C++ include directives determine compilation dependencies. -->
    <ClInclude Include="io\BitReader.h" /> <!-- Register the stream reader declaration and its cached-bit state with Visual Studio; the C++ include directives determine compilation dependencies. -->
    <ClInclude Include="models\BinaryDocument.h" /> <!-- Register the root model that owns the header and records with Visual Studio; the C++ include directives determine compilation dependencies. -->
    <ClInclude Include="models\FileHeader.h" /> <!-- Register the header model that owns its FormatInfo child with Visual Studio; the C++ include directives determine compilation dependencies. -->
    <ClInclude Include="models\FormatInfo.h" /> <!-- Register the version and tagged-flag leaf model with Visual Studio; the C++ include directives determine compilation dependencies. -->
    <ClInclude Include="models\Measurement.h" /> <!-- Register the magnitude and exponent leaf model with Visual Studio; the C++ include directives determine compilation dependencies. -->
    <ClInclude Include="models\Record.h" /> <!-- Register the record model that owns metadata and measurement children with Visual Studio; the C++ include directives determine compilation dependencies. -->
    <ClInclude Include="models\RecordMetadata.h" /> <!-- Register the ID, category, and active-flag leaf model with Visual Studio; the C++ include directives determine compilation dependencies. -->
  </ItemGroup> <!-- Close ItemGroup; following elements return to its parent. -->
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.targets" /> <!-- Load C++ build targets that compile and link the source items listed above. -->
  <ImportGroup Label="ExtensionTargets"> <!-- Group ExtensionTargets imports; entries here can extend the standard C++ build. -->
  </ImportGroup> <!-- Close ExtensionTargets ImportGroup; following elements return to its parent. -->
</Project> <!-- Close Project; following elements return to its parent. -->
