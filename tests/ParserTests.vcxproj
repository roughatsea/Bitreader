<?xml version="1.0" encoding="utf-8"?> <!-- Declare XML version and text encoding so Visual Studio and MSBuild can read the elements below. Angle syntax: The < and > bound the XML declaration; the question marks mark it as declaration syntax specifying how the following file is decoded. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003"> <!-- Define an MSBuild C++ project; its imported targets build the sources listed later in this file. Angle syntax: The < and > bound the opening Project tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <ItemGroup Label="ProjectConfigurations"> <!-- Group available build options that the configuration-specific settings below will reference. Angle syntax: The < and > bound the opening ItemGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ProjectConfiguration Include="Debug|x64"> <!-- Declare the Debug|x64 build option; its configuration and platform values are specified immediately below. Angle syntax: The < and > bound the opening ProjectConfiguration tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <Configuration>Debug</Configuration> <!-- Select Debug behavior for the build option declared by its enclosing ProjectConfiguration. Angle syntax: The < and > bound both the opening and closing Configuration tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <Platform>x64</Platform> <!-- Select the x64 CPU platform for the enclosing project build option. Angle syntax: The < and > bound both the opening and closing Platform tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </ProjectConfiguration> <!-- Close ProjectConfiguration for Debug|x64; following elements return to its parent. Angle syntax: The < and > bound a closing ProjectConfiguration tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ProjectConfiguration Include="Release|x64"> <!-- Declare the Release|x64 build option; its configuration and platform values are specified immediately below. Angle syntax: The < and > bound the opening ProjectConfiguration tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <Configuration>Release</Configuration> <!-- Select Release behavior for the build option declared by its enclosing ProjectConfiguration. Angle syntax: The < and > bound both the opening and closing Configuration tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <Platform>x64</Platform> <!-- Select the x64 CPU platform for the enclosing project build option. Angle syntax: The < and > bound both the opening and closing Platform tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </ProjectConfiguration> <!-- Close ProjectConfiguration for Release|x64; following elements return to its parent. Angle syntax: The < and > bound a closing ProjectConfiguration tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </ItemGroup> <!-- Close ProjectConfigurations ItemGroup; following elements return to its parent. Angle syntax: The < and > bound a closing ItemGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <PropertyGroup Label="Globals"> <!-- Group shared project identity and SDK properties consumed by the C++ imports below. Angle syntax: The < and > bound the opening PropertyGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ProjectGuid>{36D61DB3-E08D-4A4E-B766-587E143D06A9}</ProjectGuid> <!-- Assign a stable project identity used by Visual Studio to distinguish it from other projects. Angle syntax: The < and > bound both the opening and closing ProjectGuid tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <Keyword>Win32Proj</Keyword> <!-- Identify this as a native Windows C++ project so Visual Studio uses its C++ project tools. Angle syntax: The < and > bound both the opening and closing Keyword tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <WindowsTargetPlatformVersion>10.0</WindowsTargetPlatformVersion> <!-- Request the installed Windows 10 SDK family used by the compiler and linker imports below. Angle syntax: The < and > bound both the opening and closing WindowsTargetPlatformVersion tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </PropertyGroup> <!-- Close Globals PropertyGroup; following elements return to its parent. Angle syntax: The < and > bound a closing PropertyGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.Default.props" /> <!-- Load standard C++ defaults before defining this project's executable and toolset properties. Angle syntax: The < and > bound a self-closing Import element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <PropertyGroup Label="Configuration"> <!-- Group executable and toolset properties for the active Debug or Release build. Angle syntax: The < and > bound the opening PropertyGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ConfigurationType>Application</ConfigurationType> <!-- Build an executable application, matching the main function in the listed source files. Angle syntax: The < and > bound both the opening and closing ConfigurationType tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <PlatformToolset>v145</PlatformToolset> <!-- Select the v145 C++ compiler and linker toolset for compiling the sources listed below. Angle syntax: The < and > bound both the opening and closing PlatformToolset tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <UseDebugLibraries Condition="'$(Configuration)'=='Debug'">true</UseDebugLibraries> <!-- Use debug runtime libraries for this debug build, supporting debugger inspection of the objects. Angle syntax: The < and > bound both the opening and closing UseDebugLibraries tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <UseDebugLibraries Condition="'$(Configuration)'=='Release'">false</UseDebugLibraries> <!-- Use release runtime libraries for this release build rather than the debug runtime. Angle syntax: The < and > bound both the opening and closing UseDebugLibraries tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </PropertyGroup> <!-- Close Configuration PropertyGroup; following elements return to its parent. Angle syntax: The < and > bound a closing PropertyGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.props" /> <!-- Load compiler and platform properties after the project selects its toolset. Angle syntax: The < and > bound a self-closing Import element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <PropertyGroup> <!-- Group output-directory properties used by the test compile and link steps below. Angle syntax: The < and > bound the opening PropertyGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <OutDir>$(MSBuildThisFileDirectory)..\.build\tests\$(Platform)\$(Configuration)\</OutDir> <!-- Place the test executable under .build, organized by platform and configuration and ignored by Git. Angle syntax: The < and > bound both the opening and closing OutDir tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <IntDir>$(OutDir)obj\</IntDir> <!-- Place test intermediate files below OutDir so object files stay with the matching test build. Angle syntax: The < and > bound both the opening and closing IntDir tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </PropertyGroup> <!-- Close PropertyGroup; following elements return to its parent. Angle syntax: The < and > bound a closing PropertyGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <ItemDefinitionGroup> <!-- Set defaults for compiler, linker, and manifest tasks in this test project. Angle syntax: The < and > bound the opening ItemDefinitionGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ClCompile> <!-- Group compiler options applied to the source files listed in the project's item groups. Angle syntax: The < and > bound the opening ClCompile tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <WarningLevel>Level4</WarningLevel> <!-- Select the compiler warning level for the source files in this enclosing build-settings group. Angle syntax: The < and > bound both the opening and closing WarningLevel tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <SDLCheck>true</SDLCheck> <!-- Enable additional compiler safety diagnostics for the parser, reader, and other listed sources. Angle syntax: The < and > bound both the opening and closing SDLCheck tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <ConformanceMode>true</ConformanceMode> <!-- Enable standards-conforming C++ behavior for templates and the classes in the project. Angle syntax: The < and > bound both the opening and closing ConformanceMode tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <LanguageStandard>stdcpp20</LanguageStandard> <!-- Compile the sources as C++20, matching the project's documented language version. Angle syntax: The < and > bound both the opening and closing LanguageStandard tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </ClCompile> <!-- Close ClCompile; following elements return to its parent. Angle syntax: The < and > bound a closing ClCompile tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <Link> <!-- Group linker options that combine compiled objects into the executable. Angle syntax: The < and > bound the opening Link tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
      <SubSystem>Console</SubSystem> <!-- Build a console program so its main function and cout/cerr output work from PowerShell. Angle syntax: The < and > bound both the opening and closing SubSystem tags; the intervening text is that setting's value, and the closing slash ends the value element. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    </Link> <!-- Close Link; following elements return to its parent. Angle syntax: The < and > bound a closing Link tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </ItemDefinitionGroup> <!-- Close ItemDefinitionGroup; following elements return to its parent. Angle syntax: The < and > bound a closing ItemDefinitionGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <ItemGroup> <!-- Group related project items, such as source files, headers, or Solution Explorer folders. Angle syntax: The < and > bound the opening ItemGroup tag; attributes describe this element, and its child elements continue until the matching closing tag below. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ClCompile Include="ParserTests.cpp" /> <!-- Compile the test executable entry point and reader/hierarchy assertions as part of this executable. Angle syntax: The < and > bound a self-closing ClCompile element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ClCompile Include="..\BinaryParser.cpp" /> <!-- Compile the field-to-setter mappings and parent-child assembly functions as part of this executable. Angle syntax: The < and > bound a self-closing ClCompile element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
    <ClCompile Include="..\io\BitReader.cpp" /> <!-- Compile the implementation that consumes bits across byte boundaries as part of this executable. Angle syntax: The < and > bound a self-closing ClCompile element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  </ItemGroup> <!-- Close ItemGroup; following elements return to its parent. Angle syntax: The < and > bound a closing ItemGroup tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.targets" /> <!-- Load C++ build targets that compile and link the source items listed above. Angle syntax: The < and > bound a self-closing Import element; the slash means this single line supplies the element without a separate closing tag. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
</Project> <!-- Close Project; following elements return to its parent. Angle syntax: The < and > bound a closing Project tag, and the slash identifies the end of the matching group opened above. The angle symbols at the comment boundaries enclose this explanation as XML commentary, which the application and build tools ignore. -->
